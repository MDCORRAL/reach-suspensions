# large/sensitive data (don’t push)
# Project data directories

data/*
!data/**/
!data/dashboard_data.json
data-raw/
data-stage/
!data-stage/
# Legacy parquet artifacts that were checked in before these rules remain in history.
# Only explicitly whitelisted files below will be addable going forward.
*.parquet
!data-stage/susp_v6_teacher_features.parquet
*.duckdb
*.xlsx
*.csv
*.png
# Allow committing the built dashboard data assets
dashboard/data/
!dashboard/data/dashboard_data.json
!dashboard/data/rates_by_race_year.json
!dashboard/data/pareto_grade_setting_payload.json
# macOS system files
.DS_Store
# Cloud sync metadata (Google Drive, Dropbox, etc.)
.~lock.*
~$*
*.tmp
*.gd
desktop.ini
# R/RStudio
.Rproj.user/
.Rhistory
.RData
.RDataTmp
.Renviron

# Generated figures
outputs/*
fv_outputs/
!dashboard/data/suspension_dashboard.json
!dashboard/data/suspension_dashboard.js
